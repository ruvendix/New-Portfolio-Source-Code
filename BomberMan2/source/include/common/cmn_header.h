/*
 * <작성 날짜>
 * 2017-10-11
 *
 * <파일 설명>
 * 공용되는 헤더 파일들입니다.
 * 헤더 파일 포함 순서는 "C => C++ => 그 외의 라이브러리" 이렇게 됩니다.
 * C 헤더 파일은 C++ 헤더 파일로 사용합니다.
 * 헤더 파일은 "a ~ z" 순서로 포함됩니다.
 */

#ifndef EASIEST_COMMON_HEADER_H_
#define EASIEST_COMMON_HEADER_H_

#include <assert.h>
#include <conio.h>
#include <ctype.h>

#include <array>
#include <chrono>
#include <cstdio>
#include <list>
#include <memory>
#include <mutex>
#include <vector>

#endif